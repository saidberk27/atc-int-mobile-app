import 'package:flutter/material.dart';

class AddNewRefrigeration extends StatelessWidget {
  const AddNewRefrigeration({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(color: const Color(0xFF2DBD3A));
  }
}
